package pl.javastart.Metody.calcultor;

public class calculate {
    public static void main(String[] args) {
        double a=10;
        double b=20;

        Calculator calulator = new Calculator();
        calulator.addXY(a,b);
        calulator.divide(a,b);
        calulator.divide(a,b);
        calulator.multily(a,b);



    }
}
